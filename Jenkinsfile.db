pipeline {
  agent any

  stages {
    stage("Checkout") {
      steps {
        checkout scm
      }
    }

    stage("Deploy") {
      steps {
        sh "bash scripts/deploy.sh db"
      }
    }
  }
}
